# Copy this file to `.env` and fill in the API key (do NOT commit `.env`)
# Example:
# SEOUL_API_KEY=YOUR_REAL_KEY_HERE

# IMPORTANT: This sample must NOT contain real secrets. Replace values in your
# local `.env` (which is gitignored) when running locally.
